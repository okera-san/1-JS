
if (true){  //La condicional indica que se ejecuta siempre porque es verdadera
    let block = "Esta en un block scope";   //se declara la variable 
    console.log(block);
}

console.log(block);
// Se muestra como funciona dentro del bloque, pero no se puede acceder desde fuera limitand las variables.